# 开发者指南 (Developer Guide)

本项目的目标是提供一个高度模块化、易于维护和扩展的图像处理工具。

## 项目结构
```text
image_splitter/
├── core.py       # 核心处理逻辑 (纯逻辑，无 UI 依赖，使用 Generator)
├── cli.py        # 命令行入口 (由 argparse 提供)
├── gui.py        # 图形用户界面 (基于 tkinter 和 threading)
├── tests/        # 单元测试模块 (基于 unittest，采用参数化与数据驱动设计)
└── README.md     # 用户文档
```

## 核心设计与最佳实践

1. **坐标计算**：
   核心公式为 `left = int(j * total_width / cols)`。通过先乘后除再取整的方式，将除不尽的余数均匀分配到各个切片中，避免累积误差。

2. **UI 并发与线程安全**：
   - `gui.py` 在执行批量处理时使用 `threading` 启动后台任务。
   - **关键原则**：绝对禁止在子线程中直接读取或修改 Tkinter 变量 (如 `.get()`, `.config()`)。所有需要的参数必须在主线程中提取并作为参数传递给子线程；UI 的状态更新必须通过 `tk.after(0, callback)` 委派回主线程执行。

3. **批量处理与生成器 (Generator)**：
   - 核心的 `batch_process_images` 采用 `yield` 生成器架构，以 `(path, success, msg)` 元组的形式实时产出每个文件的处理结果。这种设计确保了 CLI 和 GUI 在调用时的一致性，且能以极低的内存开销实现进度追踪。

4. **命名引擎与防御性编程**：
   - 使用 Python 原生 `str.format` 实现。在正式进入图片处理循环前，系统会使用虚拟数据（如 `row=1, col=1`）对模板进行“试渲染”。如果模板包含非法的占位符，会立即被 `except KeyError` 捕获并中止，避免了切片到一半崩溃产生脏数据的问题。
   - 底层文件操作严格使用 `finally` 块确保资源释放 (如 `img.close()`)，防止在异常流程下发生内存或句柄泄漏。

## 测试与质量 (Testing Standards)

- **运行测试**：在项目根目录执行 `python -m unittest tests/test_core.py`
- **参数化与 DRY 原则**：严禁硬编码和重复的断言代码。通过 `subTest` 进行数据驱动测试，将所有用例（如不同尺寸组合、色彩模式、边缘情况）统一管理并交由通用的校验辅助函数验证。
- **动态校验与对齐**：当核心 API 的签名（参数组合）发生变更时，确保在 `test_cases` 数组中动态对齐所有测试用例的传入参数，确保测试环境始终覆盖所有新特性（如新增对 `RGBA` 的兼容与 `offsets` 的异常捕获）。

---

## 路线图 (Roadmap)

### 短期计划 (Short-Term Goals)
- [x] **架构解耦与安全加固**：完成生成器重构，修复 GUI 的 Tkinter 跨线程隐患。
- [x] **核心性能与防御性**：加入模板试渲染校验，UI 预览采用 `thumbnail` 极速重采样替代 `LANCZOS` 解决卡顿。
- [x] **测试重构**：实现数据驱动的全覆盖单元测试。
- [ ] **错误输出与日志**：在 CLI 和 GUI 中进一步优化错误提示信息，对于失败任务考虑加入单独的“重试”或详细日志导出功能。
- [ ] **输出覆盖控制**：加入对同名文件的冲突处理机制（提示覆盖、跳过、自动重命名）。

### 长期计划 (Long-Term Goals)
- [ ] **扩展处理管线 (Pipeline)**：
  - 支持将处理后的图片转换为特定格式（如 WebP 无损压缩、转格式输出）。
  - 支持对子图进行后处理（如加水印、打标签）。
- [ ] **复杂网格与边界**：
  - 引入边缘重叠 (Overlap) 功能，适合地图瓦片切割或全景图拼接需求。
  - 支持依据参考线 (Guide lines) 进行不规则切割。
- [ ] **多进程加速**：
  - 目前核心为单进程切图，针对巨量图片处理，可考虑将 `batch_process_images` 扩展为基于 `concurrent.futures` 的多进程架构，以完全榨干多核 CPU 性能。